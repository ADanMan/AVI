# Shared dependency ranges that keep the runtime and research stacks in sync.
numpy>=1.26.3,<3.0.0
pandas>=2.2.0,<3.0
scipy>=1.13.1,<2.0
torch>=2.4.0,<3.0
transformers>=4.44.0,<5.0

# Data and ML experiment tracking compatibility
# mlflow 2.17.1 requires pyarrow<18, so we enforce this constraint globally
pyarrow>=4.0.0,<18
